# Memolink API base URL (no trailing slash). Local dev backend:
VITE_API_BASE_URL=http://localhost:8888
# Google Maps JS API key (Maps JavaScript API + Places). Empty → location map disabled.
VITE_GOOGLE_MAPS_API_KEY=
